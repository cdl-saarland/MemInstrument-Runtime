#pragma once

/// The config header defines all kinds of settings for the runtime. It is
/// required for the common fail functionality, but can also be used, for
/// example, to configure the statistics. Find alle details in the header files
/// in "shared/include".

#define MIRT_MAX_BACKTRACE_LENGTH 16
